-- ============================================================================
-- อีเมลคนงานต่างด้าว (Worker Email) — ใช้แสดงในตารางใบงานแทนคอลัมน์ค่าบริการ
-- ============================================================================
alter table workers add column if not exists email text;
