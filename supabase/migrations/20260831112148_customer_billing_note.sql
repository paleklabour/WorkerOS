-- ============================================================================
-- Note วางบิลของนายจ้างแต่ละราย (คำแนะนำ/ข้อควรระวังตอนออกบิล เช่น
-- "ต้องส่งผ่านแผนกบัญชีเท่านั้น") — แสดงให้เจ้าหน้าที่เห็นตอนเปิดหน้ารวมบิล
-- ก่อนออกใบแจ้งหนี้ แต่ไม่พิมพ์ลงในตัวใบแจ้งหนี้ที่ส่งให้ลูกค้า
-- ============================================================================

alter table customers add column if not exists billing_note text;
