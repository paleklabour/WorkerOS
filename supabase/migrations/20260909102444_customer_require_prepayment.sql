-- ============================================================================
-- ลูกค้าบางรายต้องเก็บเงินก่อนเริ่มดำเนินการ บางรายให้เครดิตทำงานก่อนแล้วค่อยเก็บทีหลัง
-- เพิ่มธงต่อนายจ้างไว้ที่ตาราง customers — ฝั่ง client เช็คธงนี้ก่อนอนุญาตย้ายใบงาน
-- เข้าสถานะ "กำลังดำเนินการ" (ดู onKanbanDrop / saveJobForm ใน app.js)
-- ============================================================================
alter table customers add column if not exists require_prepayment boolean not null default false;
