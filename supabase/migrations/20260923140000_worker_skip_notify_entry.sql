-- ============================================================================
-- บางกรณี admin ต้องการเพิ่มคนงานเข้าระบบให้ขึ้นอยู่ภายใต้นายจ้างทันที โดยไม่ต้องรอแจ้งงาน
-- ("แจ้งเข้า") ก่อน — เพิ่มธงไว้ที่ตาราง workers เพื่อยกเว้น badge "รอแจ้งเข้า" (ดู
-- renderWorkers ใน app.js) สำหรับคนงานกลุ่มนี้ โดยไม่ต้องมีใบงานผูกอยู่จริง
-- ============================================================================
alter table workers add column if not exists skip_notify_entry boolean not null default false;
