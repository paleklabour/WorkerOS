-- ============================================================================
-- ที่อยู่จัดส่งเอกสารของนายจ้างแต่ละราย — แยกจาก "ข้อมูลที่อยู่" (branches, ใช้เป็น
-- ที่อยู่ทางธุรกิจ/ยื่นเอกสารราชการ) เพื่อใช้พิมพ์ใบปะหน้า A5 ตอนจัดส่งเอกสารทางไปรษณีย์
-- /ขนส่งเอกชน — ค่าเริ่มต้น sameAsMain=true หมายถึงใช้ที่อยู่สำนักงานใหญ่ชุดเดียวกับ branches
-- ============================================================================

alter table customers add column if not exists delivery_address jsonb default '{"sameAsMain": true}'::jsonb;
