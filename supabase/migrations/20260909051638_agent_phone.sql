-- ============================================================================
-- เพิ่มเบอร์โทรให้ Agent (ผู้ส่งงาน) — ใช้ติดต่อตอนประสานงานส่งงาน/แนะนำลูกค้า
-- ============================================================================
alter table agents add column if not exists phone text;
